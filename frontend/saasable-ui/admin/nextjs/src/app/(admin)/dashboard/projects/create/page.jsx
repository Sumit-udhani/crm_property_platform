import CreateProjectView from '@/views/projects/create';

export const metadata = {
  title: 'Create Project | SaaSable'
};

export default function CreateProjectPage() {
  return <CreateProjectView />;
}
